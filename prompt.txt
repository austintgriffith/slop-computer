Create a starter kit for building simple mobile-optimized HTML5 games or audience engagement apps using Node.js, Express, and Socket.io.

Requirements:
	•	The backend (server.js) should serve an index.html file, track and broadcast the number of connected users, and use chokidar to watch for changes in index.html and trigger a live reload via Socket.io.
	•	It should also print a local QR code in the terminal pointing to the server’s IP address, using qrcode-terminal.
	•	The frontend (index.html) should be mobile-optimized, with 100px bottom spacing to account for mobile browser UI. It should include:
	•	A “loading…” screen,
	•	A visual loading spinner,
	•	A user count indicator (👥 1),
	•	A dynamically generated QR code of the current page URL,
	•	Auto-reloading when the server tells it to reload,
	•	Automatic reconnect logic if the socket disconnects,
	•	A periodic ping (socket.emit("ping")) every 30 seconds.
	•	The backend should assign and store user IDs per socket connection and track metadata like IP, user agent, and timestamps.
	•	Ensure the server.js file is written so it can be run via nodemon server.js and restarts automatically.
	•	Add a context.txt that explains that:
	•	The backend is running via nodemon,
	•	Developers should edit but not run server.js manually,
	•	Edits to index.html will trigger auto reloads for connected users,
	•	The frontend should always be mobile-first and avoid zooming on focus.
	•	When building new engagements, always update the backend before the frontend.
